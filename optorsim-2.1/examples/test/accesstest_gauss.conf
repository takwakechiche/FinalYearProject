#
# This file contains all the parameters required for OptorSim
# Using the Properties class to store this information every space
# in the key must be preceeded by a backslash.
#
grid.configuration.file = examples/test/accesstest_grid.conf
job.configuration.file = examples/test/accesstest_gaussjobs.conf
bandwidth.configuration.file = examples/test/accesstest_bw.conf
number.jobs = 1

scheduler = 1
computing.element = 1

users = 1

optimiser = 3

dt = 1000000

access.pattern.generator = 4
shape = 0.85

computing.element = 2

initial.file.distribution = 0

fill.all.sites = no

job.delay = 2500

random.seed = no

max.queue.size = 200

auction.flag = yes
hop.count = 50
timeout = 500
timeout.reduction.factor = 0.4
auction.log = yes

background.bandwidth = no
data.directory = examples/bw_data/edg_testbed/
default.background = lyon_to_cern_ave.numbers
time.of.day = 0.0

gui = no

statistics = 3

time.advance = yes
